[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ticker-dossier"
version = "0.1.0"
description = "A safety-aware command-line agent for evidence-backed stock research"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "TickerDossier contributors" }]
keywords = ["ai-agent", "finance", "mcp", "stock-research"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: Financial and Insurance Industry",
  "Operating System :: OS Independent",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Office/Business :: Financial :: Investment",
]
dependencies = [
  "httpx>=0.27",
  "pandas>=2.0",
  "prompt_toolkit>=3.0",
]

[project.optional-dependencies]
providers = [
  "akshare>=1.16",
  "tushare>=1.4",
  "yfinance>=0.2",
]
test = [
  "pytest>=8.0",
]
dev = [
  "build>=1.2",
  "mypy>=1.17",
  "pytest>=8.0",
  "ruff>=0.12",
]

[project.urls]
Homepage = "https://github.com/nixiakT/ticker-dossier"
Issues = "https://github.com/nixiakT/ticker-dossier/issues"
Repository = "https://github.com/nixiakT/ticker-dossier.git"

[project.scripts]
ticker-dossier = "ticker_dossier.cli.main:main"
finance-agent = "ticker_dossier.cli.main:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["ticker_dossier*"]

[tool.setuptools.package-data]
"ticker_dossier.resources" = [
  "default.mcp.json",
  "skills/README.md",
  "skills/*/SKILL.md",
]

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["C901", "E4", "E7", "E9", "F"]

[tool.ruff.lint.mccabe]
# Stable application code is capped here. Two older scoring functions retain
# explicit, tighter-to-current ceilings in the reusable CI check until split.
max-complexity = 24

# Keep the first CI gate useful without forcing an unrelated repository-wide
# rewrite.  These narrow exceptions document existing debt; new files and all
# other rules in the selected families remain enforced.
[tool.ruff.lint.per-file-ignores]
"ticker_dossier/cli/main.py" = ["E702"]
"ticker_dossier/portfolio/scoring.py" = ["C901"]
"ticker_dossier/research/analysis/quality.py" = ["C901"]
"ticker_dossier/research/discovery/resolver.py" = ["F401"]
"ticker_dossier/research/discovery/web.py" = ["F541"]
"ticker_dossier/tools/learning.py" = ["F541"]
"tests/test_finance_core.py" = ["F401"]
"tests/test_finance_mcp.py" = ["F401"]

[tool.mypy]
python_version = "3.11"
files = [
  "ticker_dossier/bootstrap.py",
  "ticker_dossier/security.py",
  "ticker_dossier/runtime",
  "ticker_dossier/research/protocols.py",
  "ticker_dossier/market_data",
  "ticker_dossier/integrations/llm",
  "ticker_dossier/integrations/mcp",
  "ticker_dossier/cli/commands/models.py",
  "ticker_dossier/cli/commands/catalog.py",
  "ticker_dossier/cli/commands/custom.py",
  "ticker_dossier/cli/commands/dynamic.py",
  "ticker_dossier/cli/terminal/ui.py",
]
strict = true
follow_imports = "skip"
ignore_missing_imports = true
