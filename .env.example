# Copy this file to .env.local and fill real credentials.
# Never commit .env.local.

DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat
# If your OpenAI-compatible gateway lists different models, use one from /v1/models.
# GPT-5.6 with reasoning tools requires the Responses API:
# DEEPSEEK_MODEL=gpt-5.6-sol
# DEEPSEEK_API_MODE=responses
# DEEPSEEK_REASONING_EFFORT=xhigh
# FINANCE_MODEL_TIMEOUT_SECONDS=120
# FINANCE_MODEL_READ_RETRIES=0

# Optional CLI language: zh or en. FINANCE_AGENT_LANG remains a legacy alias.
TICKER_DOSSIER_LANG=zh

# Optional runtime approvals. Hard denials cannot be overridden.
# TICKER_DOSSIER_APPROVED_TOOLS=bash
# TICKER_DOSSIER_AUTO_APPROVE=0
# TICKER_DOSSIER_TRUSTED_MCP_SERVERS=<reviewed-config-fingerprint>

# Optional proxy for web/market-data lookups. Example for Clash/Mihomo:
# FINANCE_HTTP_PROXY=http://127.0.0.1:7897

# Optional WeChat/WeCom delivery.
# Default is dry-run: messages are written to .finance_agent/wechat_outbox.
FINANCE_WECHAT_MODE=dry-run
FINANCE_PORTFOLIO_DIR=~/.finance-agent/portfolios
# FINANCE_WECHAT_WEBHOOK=<paste-full-wecom-webhook-url-in-.env.local>
# FINANCE_WECHAT_RELAY_URL=http://127.0.0.1:8765/wechat/send

ALPHAVANTAGE_API_KEY=
TUSHARE_TOKEN=
FINANCE_ALLOW_SAMPLE_FALLBACK=0
FINANCE_QUOTE_CACHE_TTL_SECONDS=60
FINANCE_HISTORY_CACHE_TTL_SECONDS=900
FINANCE_FINANCIALS_CACHE_TTL_SECONDS=21600
FINANCE_NEWS_CACHE_TTL_SECONDS=600
